apiVersion: v1
kind: Pod
metadata:
  name: crash-demo
  namespace: dev
  labels:
    app: crash-demo
spec:
  containers:
    - name: crash-demo
      image: busybox
      command: ["sh", "-c", "echo 'crash-demo: intentional crash'; exit 1"]
  restartPolicy: Always
